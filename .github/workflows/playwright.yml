name: pw
run-name: >
  ${{ github.workflow }} :: run #${{ github.run_number }} :: ${inputs.project} :: ${inputs.priority}
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      project:
        description: 'Project'
        required: false
        default: 'chrome'
        type: choice
        options:
          - chrome
          - all
      priority:
        description: 'Priority'
        required: false
        default: 'all'
        type: choice
        options:
          - P1
          - P1-P2
          - P1-P3
          - all

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.57.0-jammy
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Playwright tests
        run: |
          TEST_ARGS=""

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # Set project flag
            if [ "${{ inputs.project }}" != "all" ]; then
              TEST_ARGS="$TEST_ARGS --project=${{ inputs.project }}"
            fi

            # Set grep flag based on priority
            case "${{ inputs.priority }}" in
              "P1")
                TEST_ARGS="$TEST_ARGS --grep @P1"
                ;;
              "P1-P2")
                TEST_ARGS="$TEST_ARGS --grep @P1|@P2"
                ;;
              "P1-P3")
                TEST_ARGS="$TEST_ARGS --grep @P1|@P2|@P3"
                ;;
            esac
          fi

          if [ -n "$TEST_ARGS" ]; then
            pnpm test -- $TEST_ARGS
          else
            pnpm test
          fi
        env:
          CI: true
          BASE_URL: ${{ vars.BASE_URL }}
          HOME: /root
        continue-on-error: true

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload HTML report as artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: playwright-report

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
